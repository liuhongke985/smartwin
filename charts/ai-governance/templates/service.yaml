apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-governance.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "ai-governance.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
